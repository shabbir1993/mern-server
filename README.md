# mern-server

API Endpoints: 

POST
`{base_url}/contact-message/add
body : {
  "name":string,
  "email":string,
  "message":string,
}`

GET
`{base_url}/contact-message` (returns contact messages data as CSV).

